# Gmail MCP Server
# No external dependencies - uses only Python standard library
# Requires Python 3.7+

# This MCP server implements:
# - OAuth 2.0 authentication flow
# - Gmail API v1 operations
# - MCP protocol over stdio

